const fs = require('fs');

module.exports = (data) => {
    let program = ``;
    data.split(/\r?\n/).forEach(function(line) {
        let content = line.trim();
        if(content !== ''){
            //do stuff kek
        }
    })
}